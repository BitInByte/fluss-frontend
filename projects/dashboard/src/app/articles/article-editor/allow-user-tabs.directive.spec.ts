import { AllowUserTabsDirective } from './allow-user-tabs.directive';

describe('AllowUserTabsDirective', () => {
  // it('should create an instance', () => {
  // const directive = new AllowUserTabsDirective();
  // expect(directive).toBeTruthy();
  // });
});
