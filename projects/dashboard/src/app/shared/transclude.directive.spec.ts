import { TranscludeDirective } from './transclude.directive';

describe('TranscludeDirective', () => {
  it('should create an instance', () => {
    const directive = new TranscludeDirective();
    expect(directive).toBeTruthy();
  });
});
